server:
  port: 8761
eureka:
  instance:
    hostname: eureka-server
  client:
    register-with-eureka: false #不把自己注册到eureka上
    fetch-registry: true        #不从eureka上获取服务信息
    service-url:
      defaultZone: http://localhost:8761/eureka/