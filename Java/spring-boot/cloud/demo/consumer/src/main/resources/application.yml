server:
  port: 8083

spring:
  application:
    name: consumer

eureka:
  instance:
    prefer-ip-address: true #注册的时候用ip注册
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
